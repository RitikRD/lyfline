import 'package:flutter/material.dart';

class Videoiconpage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("VideoIconPage"),
      ),
    );
  }
}